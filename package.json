{
  "name": "forgot-password-app",
  "version": "1.0.0",
  "description": "Complete forgot password application with rate limiting",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "cd client && npm start",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "mongoose": "^7.5.0",
    "bcryptjs": "^2.4.3",
    "nodemailer": "^6.9.5",
    "twilio": "^4.16.0",
    "express-rate-limit": "^7.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
